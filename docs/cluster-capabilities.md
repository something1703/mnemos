# Cluster capabilities

> Generated by `make db-probe` (`db/scripts/probe.py`). Do not edit by hand.
> This file is the evidence behind ADR-006. Every capability below was
> verified by executing real DDL against the live cluster, not read from docs.

**Probed:** 2026-08-07 14:22 UTC

## Cluster

| Property | Value |
|---|---|
| `version` | `CockroachDB CCL v25.3.0 (aarch64-unknown-linux-gnu, built 2025/08/14 18:25:20, go1.23.7 X:nocoverageredesign)` |
| `current_database` | `defaultdb` |
| `current_user` | `root` |
| `cluster_id` | `73737cf4-a11e-4d7a-8102-9c023029f499` |
| `default_gc_ttlseconds` | `ALTER RANGE default CONFIGURE ZONE USING range_min_bytes = 134217728, range_max_bytes = 536870912, gc.ttlseconds = 14400, num_replicas = 1, constraints = '[]', lease_preferences = '[]'` |
| `temporal_recall_window` | `14400s (~4.0h) — the hard limit on recall_as_of(); see Phase 06.3 for extending it under legal hold` |

## Capabilities

| Capability | Supported | Criticality | Used for |
|---|---|---|---|
| VECTOR column type | yes | critical | semantic_facts.embedding — the entire semantic tier (Phase 02.2) |
| VECTOR INDEX (C-SPANN) | yes | critical | prefix-scoped ANN recall; tenant isolation inside the index (Phase 02.2) |
| TSVECTOR + inverted index | yes | important | the lexical half of hybrid recall, fused with vectors via RRF (Phase 03.3) |
| Row-Level TTL | yes | important | episodic decay without an LLM or a cron job (Phase 02.2, 05.5) |
| Row-Level Security | yes | critical | tenant isolation backstop beneath the API middleware (Phase 02.6) |
| Triggers | yes | important | invariant 2 enforced BY THE DATABASE — reject any mutation lacking an audit row in the same txn (Phase 02.5) |
| PL/pgSQL user-defined functions | yes | important | the ADR-008 fallback path, and blast-radius recursive helpers |
| AS OF SYSTEM TIME | yes | critical | temporal recall — recall_as_of(), the whole Accountability pillar (Phase 03.4, 09.3) |
| Recursive CTE | yes | critical | blast_radius() transitive closure over the provenance graph (Phase 03.6) |
| SELECT ... FOR UPDATE | yes | critical | hash-chain head locking per shard (Phase 02.5, 03.8) |
| Multi-region (SHOW REGIONS) | yes | optional | REGIONAL BY ROW residency (Phase 02.3, 06.2) |
| Zone config (gc.ttlseconds) | yes | important | extending the AS OF SYSTEM TIME window for subjects under legal hold (Phase 06.3) |
| Changefeed | yes | important | the revocation bus and the console's live fact stream (Phase 02.7) |

## Verdict

Every probed capability is available. The plan runs as written.
